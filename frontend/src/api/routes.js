
// API endpoints
const host = "http://localhost";
export const auth         = host + "/auth";
export const refresh      = host + "/refresh";
export const enrolled     = host + "/enrol";
export const assessment   = host + "/assessment";
export const user         = host + "/user";
export const submissions  = host + "/submissions";
export const studentState = host + "/state/student";
