
// API endpoints
const host = "http://localhost";
export const auth_url       = host + "/auth";
export const refresh_url    = host + "/refresh";
export const enrol_url      = host + "/enrol";
export const assessment_url = host + "/assessment";
export const user_url       = host + "/user";
